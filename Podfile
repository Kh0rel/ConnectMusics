# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ConnectMusics' do
    use_frameworks!
    pod 'Alamofire', '~> 4.3'
    pod 'SwiftyJSON'

  # Pods for ConnectMusics

  target 'ConnectMusicsTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
